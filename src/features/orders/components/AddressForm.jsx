/**
 * AddressForm (orders/components) — REMOVED
 *
 * This component previously rendered the address form inside the checkout flow
 * using local state only (no API).
 *
 * It has been replaced by:
 *   src/features/address/components/AddressForm.jsx
 *
 * This file is kept as an empty re-export to prevent broken imports during
 * the migration. Once all imports are updated, this file should be deleted.
 *
 * @deprecated — import AddressForm from '@/features/address/components/AddressForm' instead
 */
export { default } from '@/features/address/components/AddressForm';
